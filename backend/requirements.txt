# ─── Web framework ────────────────────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1          # ASGI server with websocket + reload support
python-multipart==0.0.17           # Required for file upload (multipart/form-data)

# ─── Database ─────────────────────────────────────────────────────────────────
sqlalchemy==2.0.36                 # Async ORM
asyncpg==0.30.0                    # Async PostgreSQL driver
alembic==1.14.0                    # Database migrations

# ─── Validation & settings ────────────────────────────────────────────────────
pydantic==2.10.2
pydantic-settings==2.6.1           # .env file loading into Settings class
email-validator==2.2.0             # Validates email fields in Pydantic schemas

# ─── Authentication & security ────────────────────────────────────────────────
python-jose[cryptography]==3.3.0   # JWT encode/decode
passlib[bcrypt]==1.7.4             # Password hashing

# ─── Object storage (MinIO / AWS S3 / Cloudflare R2) ─────────────────────────
boto3==1.35.68

# ─── Document parsing ─────────────────────────────────────────────────────────
pypdf==5.1.0                       # Primary PDF text extractor
pdfminer.six==20231228             # Fallback PDF extractor for slide-deck PDFs
python-docx==1.1.2                 # DOCX text extractor

# ─── AI / LLM / Vector store ──────────────────────────────────────────────────
langchain==0.3.8
langchain-openai==0.2.9            # OpenAI chat + embeddings via LangChain
langchain-chroma==0.1.4            # ChromaDB vector store integration
chromadb==0.5.20                   # Local persistent vector database
openai==1.55.3                     # OpenAI Python SDK (also used with Ollama)

# ─── Rate limiting ────────────────────────────────────────────────────────────
slowapi==0.1.9                     # FastAPI rate limiting (wraps limits library)

# ─── Report generation ────────────────────────────────────────────────────────
reportlab==4.2.5                   # PDF report export
